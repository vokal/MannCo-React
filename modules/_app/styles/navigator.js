import {
  StyleSheet
} from "react-native";

import {
  colors
} from "./variables";

module.exports = StyleSheet.create( {
  nav: {
    flex: 1
  },
  wrapper: {
    backgroundColor: colors.orange,
    flex: 1,
    marginTop: 65
  }
} );
